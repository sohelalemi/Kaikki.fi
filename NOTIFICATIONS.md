# Notifications

Kaikki.fi notification center uses Supabase `notifications` rows scoped to the authenticated user. The UI shows a bell, unread badge, notification list, mark-one-read, and mark-all-read actions.

name: Apply Android home design

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Automatic Android home redesign is disabled. The mobile UI now comes only from mobile/App.js."
